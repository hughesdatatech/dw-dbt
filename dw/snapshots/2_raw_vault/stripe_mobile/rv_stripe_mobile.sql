-- Balance Transaction Fee Details
{% snapshot rv_stripe_mobile__balance_transaction_fee_details %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Balance Transactions
{% snapshot rv_stripe_mobile__balance_transactions %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Charge Groups
{% snapshot rv_stripe_mobile__charge_groups %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Charges Metadata
{% snapshot rv_stripe_mobile__charges_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Charges
{% snapshot rv_stripe_mobile__charges %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Coupons Metadata
{% snapshot rv_stripe_mobile__coupons_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Coupons
{% snapshot rv_stripe_mobile__coupons %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Credit Note Line Item Tax Amounts
{% snapshot rv_stripe_mobile__credit_note_line_item_tax_amounts %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Credit Note Line Items
{% snapshot rv_stripe_mobile__credit_note_line_items %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Credit Notes Metadata
{% snapshot rv_stripe_mobile__credit_notes_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Credit Notes
{% snapshot rv_stripe_mobile__credit_notes %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Customers
{% snapshot rv_stripe_mobile__customers %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Disputes Metadata
{% snapshot rv_stripe_mobile__disputes_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Disputes
{% snapshot rv_stripe_mobile__disputes %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Early Fraud Warnings
{% snapshot rv_stripe_mobile__early_fraud_warnings %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Exchange Rates from USD
{% snapshot rv_stripe_mobile__exchange_rates_from_usd %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Invoice Items Metadata
{% snapshot rv_stripe_mobile__invoice_items_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Invoice Items
{% snapshot rv_stripe_mobile__invoice_items %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Invoice Line Items
{% snapshot rv_stripe_mobile__invoice_line_items %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Invoices Metadata
{% snapshot rv_stripe_mobile__invoices_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Invoices
{% snapshot rv_stripe_mobile__invoices %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Payment Intents Metadata
{% snapshot rv_stripe_mobile__payment_intents_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Payment Intents
{% snapshot rv_stripe_mobile__payment_intents %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Payment Reviews
{% snapshot rv_stripe_mobile__payment_reviews %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Plans Metadata
{% snapshot rv_stripe_mobile__plans_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Plans
{% snapshot rv_stripe_mobile__plans %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Prices Metadata
{% snapshot rv_stripe_mobile__prices_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Prices
{% snapshot rv_stripe_mobile__prices %}

  {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Products Metadata
{% snapshot rv_stripe_mobile__products_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Products
{% snapshot rv_stripe_mobile__products %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Refunds Metadata
{% snapshot rv_stripe_mobile__refunds_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Refunds
{% snapshot rv_stripe_mobile__refunds %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Rule Decisions
{% snapshot rv_stripe_mobile__rule_decisions %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Subscription Items
{% snapshot rv_stripe_mobile__subscription_items %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Subscriptions Metadata
{% snapshot rv_stripe_mobile__subscriptions_metadata %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Subscriptions
{% snapshot rv_stripe_mobile__subscriptions %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Tax Rates
{% snapshot rv_stripe_mobile__tax_rates %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Transfer Reversals
{% snapshot rv_stripe_mobile__transfer_reversals %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}

-- Transfers
{% snapshot rv_stripe_mobile__transfers %}

    {{ build_snapshot_model(this.name) }}

{% endsnapshot %}
