version: 2

models:
  - name: br_opioid_stay
    columns:
      - name: county_name
        tests:
          - unique
          
  - name: br_pagov__opioid_stays
    columns:
      - name: county_name
        tests:
          - unique
